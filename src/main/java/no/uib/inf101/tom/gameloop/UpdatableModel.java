package no.uib.inf101.tom.gameloop;

public interface UpdatableModel {
    
    /**
     * updates the model (makes stuff move and cutscenes go to the next frame). 
     */
    public void update();
}
